import styled from "styled-components";

const StyledActionWrapper = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 1rem;
    justify-content: center;
`;

export default StyledActionWrapper;